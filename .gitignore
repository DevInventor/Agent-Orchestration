# Agent-Orchestration run artifacts (created wherever /ship runs)
pipeline/

# a user's own registry may contain local machine paths â€” keep it out of the plugin repo
agent-orchestration.config.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# test_pipe.py imports pipe.py as a module
__pycache__/
# Screenshot and snapshot artefacts from browser tooling - never part of the plugin.
.playwright-mcp/
*.png
